{
  "title": "Search to Markdown",
  "description": "Search Google using SERP API and convert results to markdown format",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "The search query to execute on Google",
      "editor": "textfield",
      "example": "artificial intelligence trends 2024"
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Number of web pages to scrape and convert",
      "editor": "number",
      "default": 5,
      "minimum": 1,
      "maximum": 20
    },
    "getRecent": {
      "title": "Get Recent Results",
      "type": "boolean",
      "description": "If true, prioritize recently published content. If false, get best hits regardless of publish date",
      "default": false
    },
    "countryCode": {
      "title": "Country Code",
      "type": "string",
      "description": "Two-letter country code for search localization (e.g., US, UK, DE, FR)",
      "editor": "textfield",
      "default": "US",
      "example": "US"
    },
    "languageCode": {
      "title": "Language Code",
      "type": "string",
      "description": "Two-letter language code for search results (e.g., en, es, fr, de)",
      "editor": "textfield",
      "default": "en",
      "example": "en"
    },
    "unwantedTags": {
      "title": "Unwanted tags",
      "type": "array",
      "description": "Array of HTML tag names to remove during cleaning",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "default": ["head", "img", "script", "style", "meta", "link", "noscript", "iframe", "embed", "object"]
    },
    "unwantedAttrs": {
      "title": "Unwanted attributes",
      "type": "array",
      "description": "Array of HTML attribute names to remove during cleaning",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "default": ["style", "class", "id", "onclick", "onload", "onerror", "data-.*", "width", "height", "valign", "role", "align", "cellspacing", "border", "cellpadding", "aria-.*"]
    }
  },
  "required": ["searchQuery"]
}